# 静的ファイルの保存期間の指定。
# /_next/static/ 配下のファイル名には内容ごとの識別子が入っており、
# 中身が変わればファイル名も変わる。そのため一度読み込んだものは
# 作り直しても混ざらないので、長期間ブラウザに持たせてよい。
# （既定のままだと毎回サーバーに問い合わせが走り、2回目以降の表示が遅くなる）
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
