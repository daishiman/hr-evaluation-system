# 残課題リスト

最終更新: 2026-08-12（制度マスタにイベントストア（誰が・いつ・何を変えたかの記録）を追加）

初回リリースに入れなかったもの（＝やらないことではなく、順番を後にしたもの）と、
制度として未確定のまま「叩き台の初期値」で動かしている論点をまとめる。

書式: `- [分類] 内容 — 後回しにした理由`

---

## A. 未確定の制度論点（システム上どう仮置きしているか）

いずれも **コードにハードコードしていない**。データベースのマスタに入れてあり、
管理画面（`/admin/masters`・`/admin/masters/promotion`・`/admin/behavior`・`/admin/scheme`・`/admin/kgi`・`/admin/raises`）に責務ごとに分けてあり、各項目の「変更する場所」から変更できる。
画面上は「仮」バッジ（`ProvisionalMark`）と注記で仮置きだと分かるようにしてある。
管理画面から保存し直すと仮置きの印は外れる。

| # | 未確定の論点 | 仮置きした値 | 変更する場所 |
|---|---|---|---|
| 1 | 昇格に必要な行動指針の点数（B→R など6区間） | B→R:10点、R→C:8点、C→AM:12点、AMⅠ→AMⅡ:12点、AM→M:12点、MⅠ→MⅡ:12点 | `/admin/masters/promotion`（昇格の条件・要件） |
| 2 | 昇格に必要なKPIの点数 | 全区間 100点満点中100点（＝全項目A） | `/admin/masters/promotion`（昇格の条件・要件） |
| 3 | ~~8項目の配点（合計100点）~~ **確定済み（2026-08-11 / v11）** | 等級区分ごとに配点の型を固定した。等級要件達成率＝Beginner 100 / Regular 80 / Chief 40 / AM 30 / Manager 20点、Chief 以上は金銭系（単価率・売上達成率・利益率）から1つを20点枠、残りは1項目10点。選ぶ項目数は 1 / 3 / 6 / 7 / 8。どの等級区分でも合計100点 | `/admin/scheme`（等級区分タブ）／型そのものは `grade_point_rules` |
| 4 | ランク（A〜E）を点数に換算する比率 | A=100%、B=80%、C=60%、D=40%、E=0% | `/admin/scheme`（比率）／判定は確定時にスナップショット |
| 5 | 昇給額（等級別・月額） | Beginner 3,000円 〜 Manager Ⅱ 10,000円（半期＝6ヶ月分で年額換算） | `/admin/raises`（昇給の設定） |
| 6 | 事業所KGI達成係数 | 121%以上=1.5 / 111〜120%=1.2 / 100〜110%=1.0 / 95〜99%=0.6 / 90〜94%=0.4 / 89%以下=0.2 | `/admin/kgi`（事業所KGIの達成率）の「達成係数を変更する」 |
| 7 | 元の表で「新規（素案）」と書かれていたKPI項目 | 素案のまま取り込み、項目側に仮置きの印を付けた | `/admin/scheme` で確認できる。KPI項目そのものの編集画面は未配置 |
| 8 | ~~Beginner にもKPI8項目を適用するか~~ **確定済み（2026-08-11 / v11）** | **Beginner は等級要件達成率1項目だけで100点**。他の項目は「0点」ではなく**評価対象外**として行を作らない（0点で並べると「評価されなかった」が「0点だった」に化けるため） | `/admin/scheme`（等級区分タブ） |
| 9 | 半期の目標上限数（等級ごと） | 等級マスタの `targetCap` に初期値を投入 | `/admin/masters` 等級設定 |
| 10 | AM Ⅰ・AM Ⅱ に行動指針（5問）を出すか（元データが食い違っていた） | **実際に使われていたアンケート用紙に合わせて出す**（等級3〜4の基準を適用） | `/admin/behavior`（行動指針）で会社ごと・等級ごとに切り替え可能。画面に食い違いの注記あり |
| 11 | 等級要件達成率の数え方 | **確定済み（2026-08-10）**。`達成した項目数 ÷ そのアンケートで実際に出題した等級要件の項目数`。未回答は未達として分母に残す。元の制度（半期の目標上限数で割る）とは考え方が変わっている（→ `docs/migration-mapping.md` §9-6）| ランク基準（100%以上=A 〜 40%未満=E）は **初期値の仮置き**。現時点で実運用データが1件も無く、妥当性は検証できていない（→ `docs/migration-mapping.md` §9-6）。変更は `/admin/scheme` |

補足:
- 昇格に必要な点数は **アンケート回答画面には一切出していない**（APIレスポンスからも除外）。
  閲覧できるのは MANAGER 以上の `/criteria` と管理画面だけ。
- 確定済みの評価は、判定した時点の配点・閾値をスナップショットとして持っている。
  上の値を後から変えても **過去の結果は動かない**（画面にもその旨を明記している）。

---

## B. 機能

- ~~[機能] アンケートの中身を確認できるのが会社の管理者だけ~~ — **解消済み（2026-08-11）**。`/forms`（一覧）と `/forms/[id]`（1本の中身）を追加し、**どのロールでも自社のアンケートを、どの等級あてのものでも読める**ようにした。設問文・補足・必須/任意・答え方・選択肢を確認専用で表示し、入力欄が無いので開いても回答・下書きは作られない。**回答（誰がどう答えたか）は従来どおり会社の管理者以上だけ**、作成・編集・公開・締め切りも従来どおり（判定は `src/lib/session.ts` の `canSeeFormContent` / `canSeeFormResponses` / `canEditForm` に集約し、`src/lib/session.test.ts` で固定）。一般の方には配点と昇格ゲートを渡さない（`src/lib/domain/form-visibility.ts`）
- ~~[不具合] 「評価を集計し直す」ボタンが動かない~~ — **解消済み（2026-08-11）**。受け口 `src/app/api/evaluations/build/route.ts` を追加し、既存の集計処理（`src/lib/evaluate.ts` の `buildEvaluationsForCycle`）をそのまま呼ぶようにした。**確定済みの評価は計算にも保存にも進まず据え置き**、何人ぶん据え置いたかを実行後の文言に出す（`src/lib/domain/build-summary.ts`）。実行できるのはマネージャー以上で、期（サイクル）を自社に絞ってから集計するため他社のデータには及ばない。3つのボタンはいずれも実行前の確認文を持つ
- [機能] アンケートの中身を**ログインなしで**共有する（レビュー用の読み取り専用URL） — 今回はログイン必須のままにした。社外の人に見せる必要が出てから。中身とはいえ社内の評価設問なので、URLを知っていれば誰でも読める状態は運用の合意なしに作らない
- [機能] アンケートの中身に対するコメント（「この設問は分かりにくい」の指摘を画面上で残す） — 今回は読めるようにするところまで。指摘は口頭・チャットで受ける運用が先
- [機能] 実運用データが一定件数溜まった段階で、等級要件達成率を含む閾値（ランク基準A〜E・KGI係数・昇給判定）の分布を再検証する — 移行時点で過去の実データが1件も無く（元シートの回答は氏名が `test` の動作確認のみ）、現在の閾値は妥当性を検証できないまま初期値を仮置きしている。最初の半期の評価が確定したら、実際の達成率分布を出してランクが偏っていないかを確認し、必要ならランク基準は `/admin/scheme`、KGI係数は `/admin/kgi`、昇給は `/admin/raises` で調整する
- [機能] Google ログイン（Better Auth の Google 連携） — OAuth クライアントIDを用意できていないため。テーブル構成は Google 連携をそのまま追加できる形にしてある
- [機能] アンケート設問のドラッグ＆ドロップ並べ替え — 現在は ↑↓ ボタン。触れる範囲が明確でタッチ端末でも扱いやすいため、初回はこちらを採用（等級要件・昇格要件の並べ替えも同じく ↑↓ ボタン）
- ~~[機能] 行動指針（5観点とレベルごとの文言）の編集画面~~ — **解消済み（2026-08-11、等級割当UIは2026-08-12に統一）**。`/admin/behavior` で、各等級の行ごとに「この等級に出す行動指針」または「出さない」を選んで保存し、観点の利用状態、レベル名・説明文も変更できる。変更は次に作るアンケートから反映し、作成済み・公開済みアンケートと既存評価は動かさない
- ~~[機能] 行動指針の基準セットが2つ（等級1〜2／等級3〜4）で固定~~ — **解消済み（2026-08-11）**。`/admin/behavior` で基準セットを**会社ごとに作れる**ようにした（既存の複製／空から作成／呼び名の変更／使用停止・再開、観点の追加）。呼び名の初期値も等級名にそろえた（`Beginner・Regular向け` / `Chief・AM向け`）。すでに公開したアンケートと確定済みの評価は動かない
- [機能] 基準セットの並べ替え（画面上での上下移動） — いまは作った順に並ぶ。2〜3セットのうちは並べ方で困らないため、増えてから
- ~~[機能] 追加した観点の削除~~ — **解消済み（2026-08-12）**。一度もアンケートに出しておらず、確定済みの評価にも入っていない観点は「完全に消す」で消せる（5段階の文章も一緒に消える）。使った実績があるものは消せず、どこで使っているかを画面に出す
- [機能] 追加した観点の並べ替え — 追加は末尾に入る。順番を入れ替えたい要望が出てから（等級要件・昇格要件と同じ ↑↓ ボタンで揃える）
- ~~[機能] 基準セットの完全な削除~~ — **解消済み（2026-08-12）**。「どの等級にも出しておらず、中の観点を一度も使っていない」基準セットは中身ごと消せる。判定は `src/lib/master-usage.ts` で数え、`DELETE /api/masters` 側でも必ず数え直す（画面のボタンの出し分けだけに頼らない）
- [機能] 評価結果のPDF・Excel出力 — 画面で見て判断する運用が先。出力要望が出てから
- [機能] 未回答者への催促メールの自動送信 — メール基盤の準備が必要なため。**未回答者の可視化そのものは実装済み**（回答一覧画面は提出済みだけを並べず、対象等級の在籍者を全員並べて「未回答」バッジと未回答件数を出す）。催促は同じ画面の「未回答の方への連絡文をコピーする」ボタンで文面を作り、メールやチャットに貼って送る運用
- [機能] 評価コメントの複数人レビュー（1次評価・2次評価の分離） — 現在の運用（上長1人が評価）を先に固める
- [機能] KPI項目のカテゴリ自体の追加・削除 — 現在は7カテゴリ固定（項目の入れ替えは可能）
- [機能] 一般の方から上長へのフィードバック依頼 — 業務フローに入っていないため
- [機能] マネージャー・会社の管理者のホームに「自分の未提出アンケート」を出す — 2026-08-11 にサイドバーへ「自分の評価」（実績を報告する／評価の結果を見る）を足して、たどり着けない状態は解消した。ただし `/manager` のホームは担当チームの作業だけを並べており、自分自身の未提出は出ない。誰の画面にも自分の締切が出ないため、当面はサイドバーから自分で開いてもらう
- [機能] 等級5（Manager Ⅰ / Ⅱ）に行動指針を出したくなったときの設定 — いまは行動指針の帯域が2つ（等級1〜2 / 等級3〜4）しか無く、等級5には帯域そのものを割り当てられない（`/admin/behavior` の選択肢に出ない）。等級5は「行動指針は対象外・KPIと等級要件達成率で100点満点」という設計で、評価されないわけではない。マネージャーにも行動指針で評価したくなったら、帯域を1つ増やす作業が要る
- [機能] 対象者の一覧から利用停止の方を外す — 未提出者の一覧と回答状況は在籍状態を見ておらず、利用停止にした方も対象として並ぶ。運用上は退職者が期の途中で出たときに気になる
- [機能] 名簿の取り込みで、未登録の等級・事業所を自動で作る — 誤字がそのまま新しい等級として増えると危険なため、いまは「その名前の等級が見つかりません」と行ごとに止める。等級・事業所は管理画面で先に作る運用
- [機能] 参考配点の「元の等級区分」を自動で当てる — いまは評価セット画面で等級区分（Beginner／Regular／Chief／AM／Manager）を人が選んでから読み込む。評価セットと等級の対応づけを持たせれば自動選択にできるが、対応が1対1でない会社があるため運用を見てから
- [機能] 参考配点の一括読み込みの取り消し（Undo） — いまは読み込んでも保存するまで確定しないため、画面を離れれば元に戻る。保存後の巻き戻しが要るようになったら
- [機能] 評価全体の「暫定」表示（制度上の配点合計より、基準が整備済みの配点が少ないときに暫定と示す） — 現行GASが持っていた表示。いまは項目ごとの「仮」バッジで代替している。評価セットが合計100点で組まれている限り差は出ないため後回し
- [機能] 取り込みの取り消し（直前の一括取り込みをまとめて戻す） — 取り込み前に「まず内容を確認する」で結果を確かめられるため、初回はそちらで代替

## C. UX

- ~~[UX] 画面ごとに見出し・呼び名・固定表示がばらついている~~ — **解消済み（2026-08-11 / 全画面のUI/UX見直し）**。①サイドバーの語を用語の正本にし、全画面の見出しを一致させた（→ `docs/product/spec.md` §5-4 用語表。「評価サイクル」「採点の基準」「8項目」といった言い方は画面から無くした）②縦に長い画面は見出しの帯（対象者・期間・等級・状態の札つき）を固定し、主要な保存・提出ボタンは画面下の帯に固定した（実装は `PageTitle` の `sticky` と `components/layout/StickyActionBar.tsx` の2箇所だけ）③下位の画面には「一覧に戻る」ボタンではなくパンくずを付けた ④サイドバーは分類の切れ目にだけ罫線を引き、分類の中の項目を字下げした
- ~~[UX] 何でも表形式で出していて読みにくい~~ — **解消済み（2026-08-11 / 全画面のUI/UX見直し）**。①「表を使う／使わない」の判断基準を決めて全画面に当てた（→ `docs/product/spec.md` §5-5。棚卸しの結果、10箇所のうち3箇所を表からカードに置き換えた）②置き換え先を共通部品にした（`RecordList`＝カードの一覧／`DefList`＝1件の中身／`StatGrid`＝件数の並び。画面ごとに組み方を書き起こさない）③表は `DataTable` 1箇所に集約し、列見出しがスクロールで消えないようにした ④狭い画面では表が自動でカードに畳まれるようにした（横スクロールを無くした）⑤画面に `<table>` `<dl>` を直接書くと落ちるテストを入れて、作法が崩れないようにした
- ~~[UX] 同じ形の行・カードの頭を画面ごとに手で組み直していた~~ — **解消済み（2026-08-11 / 全画面のUI/UX見直し）**。①一覧の1行を `CardRow`、カード1枚の頭を `CardHead` の2部品に集約し、19画面・8画面をそれぞれ置き換えた（→ `docs/product/spec.md` §5-5）②並び順（見出し → 補足 → 右の数値 → 状態の札）を部品側で固定し、画面ごとに入れ替えられないようにした ③この過程で見つかった読みにくさを2件直した（設問と回答の対が細い右列に押し込まれていた／評価メモの長い文章が1行に潰れていた）④手で組み直すと落ちるテストを追加した（例外は理由付きの許可リストに限定）
- ~~[UX] ボタン・見出し・確認の出し方が画面ごとにばらついていた~~ — **解消済み（2026-08-11 / 全画面のUI/UX見直し）**。全34画面を1画面ずつ点検した結果として、①ボタンを3種類（その場で動く／画面が変わる／ファイルを受け取る）に整理し、手書きしていた30箇所を置き換えた ②節見出しを1つの部品に集約し、手書きの見出し9箇所を置き換えた ③「使わなくする」操作の確認が2画面だけブラウザの素っ気ない確認窓だったのを、他の画面と同じ「その場で内容を読んでから押す」形にそろえた
- ~~[UX] 小さすぎる文字・薄いボタン色・指で押しづらい大きさ~~ — **解消済み（2026-08-11 / 全画面のUI/UX見直し）**。①10〜11pxの文字（状態の札・補足・判定範囲の帯）を全て12px以上にした ②主要ボタンのオレンジが白文字とのコントラスト基準（4.5:1）を満たしていなかったため、一段濃い色に変更した ③スマートフォン・タブレットでは、押せるものが44px以上になるようにした（パソコンの一覧の密度は変えていない）
- [UX] 全画面をスマートフォン実機（375px幅）で通し確認する — 幅ごとの見え方はブラウザの幅変更で確認済み。実機のキーボード表示時に固定した帯がどう見えるかは、運用開始前に一度確認したい
- ~~[UX] `/account/password`（パスワードの変更）の見直し~~ — **解消済み（2026-08-11 / 全画面のUI/UX見直し）**。作業環境の設定でこのフォルダを開けず後回しになっていた画面。①ほかの画面と同じ作法（パンくず・見出し・ボタン部品）に揃えた ②**パスワードの欄がどの画面でもブラウザ既定の枠のままだった**のを共通の見た目に直した（ログイン画面も同時に直った。メール欄と枠の形が違っていた）③満たすべき条件（文字数・いまのものと違う・確認用と同じ）を打ち込む前から出し、満たすたびにその場で印が付くようにした ④「表示する／隠す」を文字のボタンで付けた ⑤変更後は入力欄を消して結果と次の導線だけを出すようにした ⑥二重送信を防いだ ⑦画面の条件とサーバーの判定がずれたら落ちるテストを入れた（`src/components/PasswordChangeForm.test.ts`）
  - 帯（画面下に固定する操作の帯）は**あえて付けていない**。入力欄が3つで狭い画面でもボタンが視野から出ず、付けると本文とボタンが二重になるため（→ `docs/product/spec.md` §6 の「適用しない画面と理由」に記録）
- ~~[UX] KPI・評価セットの画面で、KPI項目を選び直せず初期値のままになる~~ — **解消済み（2026-08-11 / KPI・評価セットの作り直し）**。選べる件数の上限に達すると、選んでいない項目のボタンを**すべて押せない状態**にしていた。初期値がすでに枠を満たしているため、開いた時点で候補が全部灰色になり「選べない・初期値のまま」に見えていた。上限に達しても押せるようにし、超えたときは「◯件外してください」と出して保存だけを止める形に変えた。いま選んでいる項目は「外す」ボタン付きで別枠にまとめ、差し替えの手順が分かるようにした
- ~~[UX] KPI・評価セットの基準設定に、選んでいない項目まで並ぶ~~ — **解消済み（2026-08-11 / KPI・評価セットの作り直し）**。基準の一覧が評価セット全体（5つの等級区分ぶん）を読んでいたため、その等級区分で選んでいない項目や、同じ項目の重複したカードが並んでいた。手順2の画面では**その等級区分で選んだ項目だけ**を選んだ順に出し、先頭に「この等級区分向けの基準が決まっていない項目が◯件」を出すようにした
- ~~[UX] KPI・評価セットが1画面に詰め込みすぎで、何をする画面か分からない~~ — **解消済み（2026-08-11 / KPI・評価セットの作り直し）**。1つの画面で「等級区分の切り替え・配点の確認・項目の選択・基準の編集・KPIの比較・共通設定」を同時にやらせていた。**入口（どの等級区分を設定するか）→ 手順1（使うKPIを選ぶ）→ 手順2（選んだ項目の基準を決める）→ 次の等級区分へ**に分け、各画面に「ステップ n/2」「等級区分 n/5」と次の行き先を出すようにした。全等級区分が終わると評価期間の設定へ案内する
- [UX] KPIのA〜E基準は、いまも**項目ごとに1組**（全等級区分で共通） — 手順2の画面にその旨を明記した。等級区分ごとに違う線引きにしたい場合は、いまは別のKPI項目として登録する必要がある。制度として本当に等級ごとに変えたいのかを確かめてから、`kpi_rank_criteria` の持ち方を変える（既存データの移行が要る）
- [UX] KPI項目そのもの（名前・単位・計算式）を足す・直す画面がまだ無い — 選ぶ・基準を決めるところまでは画面から行えるが、項目自体の追加は初期データの投入で行っている
- [UX] アンケート回答の途中保存（下書き）をサーバー側にも持つ — 現在はブラウザ内保存のみ。端末をまたぐ再開が必要になったら
- [UX] メンバー一覧の絞り込み・並べ替えの保存 — 件数が数十人規模のため、初回は不要と判断
- [UX] スマートフォンでの評価入力の最適化 — 評価入力はPC運用が前提。閲覧は各画面ともスマートフォン幅で確認済み
- [UX] 一覧の一括操作（複数人の評価をまとめて確定） — 誤操作の影響が大きいため、まず1件ずつの運用で様子を見る
- ~~[UX] スマートフォンでの「操作する会社」切り替え~~ — **解消済み（2026-08-11 / v7）**。サイドバー導入にあわせて「操作する会社」をサイドバー内へ移し、スマートフォンでも引き出しメニューから切り替えられるようにした

- ~~[UX] 数字を入れる欄の作法が画面ごとにばらついている（全角・貼り付け・空欄の扱い）~~ — **解消済み（2026-08-12 / 数値入力の統一）**。数値の欄15箇所をすべて共通部品（`src/components/NumberField.tsx`）に通した。①全角で打たれても黙って半角にする（叱らない）②表計算から貼り付けたときのカンマ・空白・`%` も黙って落とし、カーソルは貼った文字の直後に残す ③打っている最中は形を見ない（「-」だけ・「1.」まででも消さない）④**空欄を0にしない**（空欄＝制限なしの欄があるため）⑤マイナス・小数・上下限は欄ごとに決める（行動指針の -1 点、達成率の小数が入らなくなるため一律禁止にしない）⑥スマートフォンで数字のキーボードが出る ⑦同じ判定を受け口（API）にも置いた。作法が崩れたら落ちるテストを入れた（`src/lib/domain/number-input.test.ts` / `src/components/ui-rules.test.ts`）
- ~~[UX] 提出が断られたのに「提出済み」の印だけが残り、本人が二度と直せなくなる~~ — **解消済み（2026-08-12 / 数値入力の統一の作業中に発見）**。回答の受け口が「提出済みに書き換える → 未入力の項目を数える」の順で動いていたため、未入力があって断られた提出でも状態だけが提出済みになり、次からは「すでに提出済みのため変更できません」で本人が入力できなくなっていた。**検査を書き換えの前に移した**。本番データを確認したところ、この状態になっている人は0人（`docs/product/spec.md` の回答保存の順序として記録）
- [UX] **アンケートの設問に「整数だけ」の印が無い** — 設問が持っているのは最小値・最大値・単位だけで、「件」「人」のように数え上げる単位でも小数を止められない。いまは小数を許している（勝手に整数へ丸めると、回答者が打った値と保存される値が食い違うため）。設問の作りに印を足すか、単位から決めるかを制度側と決めてから直す
- [UX] アンケートの回答欄で「最大値」を画面に出していない — 最小値は「◯以上の数字を入力してください」と出しているが、最大値は設問側にまだ1件も設定されていないため（本番データで確認済み）表示を用意していない。設定を使い始めたら出す
- ~~[UX] ランク基準の下限・上限に、当てはまる値が1つも無い組を保存できてしまう~~ — **解消済み（2026-08-12 / 数値入力の統一）**。下限＞上限（および下限＝上限。上限はその値を含まないため空の範囲になる）を、画面では送る前に、受け口では保存前に断るようにした。本番データに該当する組は無い（確認済み）

## D. セキュリティ

- [セキュリティ] 監査ログ（誰がいつ何を変更したか） — **金額や判定に直結する2つ（昇給額・事業所KGIの達成率）については、専用の変更履歴が実装済み**（`raise_revisions` / `office_kgi_revisions`）。等級要件・昇格要件は新idと `previous_version_id` で内容の版を追えるようにしたが、これは「何がどの版から変わったか」の系譜であり、actor・理由を持つ監査ログではない。残っているのは、配点・ランク基準を含む制度マスタ全般について「誰が・いつ・なぜ」を記録する横断監査。すべてをイベントストアへ移すのではなく、変更事故の実例と運用要件が出た対象から既存の専用履歴と同じ作法で追加する
- [セキュリティ] レート制限の細かい調整 — ログイン試行の制限は有効（短時間に繰り返すと「しばらく待ってからやり直してください」になる）。回答送信・書き出しなど他の操作への制限追加は運用を見てから
- [セキュリティ] 二要素認証 — 同上
- [セキュリティ] パスワード変更画面の試行回数の制限 — ログイン画面には制限があるが、変更画面の「いまのパスワード」欄には無い。ログイン済みの本人しか到達できないため優先度は低い
- [セキュリティ] 仮パスワードのままの利用を一定期間で強制的に止める — いまは全画面上部で変更をお願いし続ける表示のみ（強制ログアウトはしない）。運用が始まってから期限の要否を決める
- [セキュリティ] 下書き（未公開）のアンケートも全ロールから読める — 「配る前に内容が適切かを確かめる」ことが目的なので、公開前こそ読めるべきと判断した。一般の方にも見えるため、画面には「まだ配っていない下書きです。公開までに設問が変わることがあります」と理由を出している。**下書きは管理者・マネージャーだけに限る**運用にしたくなったら、`canSeeFormContent` を1箇所直せば全画面に効く
- [セキュリティ] 権限ロールの細分化（閲覧専用の人事担当など） — 4ロールで足りるかを実運用で確認してから
- ~~[セキュリティ] マネージャーが「自分自身の評価」を自分で確定できてしまう~~ — **解消済み（2026-08-11）**。`/api/evaluations/[id]` で、対象者と操作している人が同じ場合は確定・差し戻し・コメント保存をすべて 403 で拒否する（判定は `src/lib/domain/evaluation-authority.ts` に集約し、`src/lib/domain/evaluation-authority.test.ts` で固定）。**役割では例外を作らない**（会社の管理者も自分自身の評価には手を入れられない）。画面（`/manager/evaluations/[id]`）でも確定・差し戻し・コメント欄を出さず、理由と「誰に頼めばよいか」を表示する。**閲覧は従来どおり**（自分の結果を見るのは本人の権利で、「自分の評価」メニューを壊さないため）。他人の評価の確定は従来どおり可能。会社の管理者が1人だけの会社では、その人自身の評価はシステム全体管理者が会社を選んで確定する経路が残っている
- [セキュリティ] 自分の評価が「確定されないまま」になっていないかを誰かが気づける仕組み — 上の変更で、自分の評価は必ず他の人に確定してもらう必要が出た。会社の管理者のホームに「確定を頼まれている評価」を出すのが次の一手
- [セキュリティ] `pnpm audit` の moderate 1件（esbuild、開発サーバー向けの指摘。drizzle-kit 経由の開発用依存で、公開されるアプリ本体には含まれない） — 上位パッケージの更新待ち
- [セキュリティ] HSTS（`Strict-Transport-Security`）ヘッダーの付与 — workers.dev ドメインでは常時HTTPSのため実害は小さい。独自ドメインに移すときに合わせて設定する
- [セキュリティ] Content-Security-Policy の `'unsafe-inline'` を外す — Next.js の仕組み上インラインスクリプトが必要なため、nonce 方式への切り替えは後回し

## E. 性能

- [性能] 一覧のページング — 1社あたり数十人規模を想定。数百人規模になったら
- [性能] 集計結果のキャッシュ — 実測で問題が出てから
- [性能] 公開ファイルの容量監視 — 無料プランの上限は圧縮後3MB。ログインとDBの部品を共通化して 1.8MB まで下げた（2026-08-10 時点。v10 で 1,981KB）。画面が増えて上限に近づいたら、公開前の確認に容量チェックを組み込む
- [性能] ブラウザ実機での体感速度の計測（Core Web Vitals: LCP・CLS・INP） — 計測用のブラウザ連携（chrome-devtools MCP）が未設定のため未実施。本番URLの応答時間（下記）は実測済み
- ~~[性能] **参考データを最初から全部送っているため、一部の画面が重い**~~ — **解消済み（2026-08-11 / v10）**。制度マスタのランク基準（8項目×A〜E＝40件）は折りたたみにして開いたときに `/api/masters/rank-criteria` から読むようにし、評価セットの参考配点は「元の配点を表示する」を押したときに、選んでいる等級区分のぶんだけ `/api/reference-points?group=…` から読むようにした（全件800行超のうち50行だけ）。共通の読み込み処理は `src/components/useLazyJson.ts`。**結果（本番実測）: 制度マスタ 111KB → 42KB、評価セット 91KB → 46KB**。開いたときに追加で読む量はそれぞれ 6KB / 4KB。保存したあとは控えを捨てて読み直すため、開き直したときに古い値を上書きしてしまうことはない
- [性能] 使われなくなった読み込み口が2つ残っている（`/api/reference-points`、`src/components/useLazyJson.ts`） — KPI・評価セットの作り直しで、ランク基準を「開いたときだけ読む」形をやめた（その等級区分で選んだ項目だけ＝最大8項目になり、最初から並べたほうが未設定に気づけるため）。あわせて `/api/masters/rank-criteria` は削除したが、残る2つは同時期の別作業と衝突しうるため据え置いた。使う画面が無いことを確かめてから消す

### 実測値（2026-08-10・本番URL・移行版デプロイ後）

| 画面 | 応答時間 | 画面の大きさ |
|---|---|---|
| ログイン | 283ms | 15KB |
| 管理者ホーム | 146ms | 15KB |
| 昇給の設定 | 124ms | 32KB |
| 評価セット | 110ms | 35KB |
| 評価基準の確認 | 194ms | 49KB |
| 制度マスタ | 314ms | 101KB |

### 実測値（2026-08-10・v4 デプロイ後・追加分）

| 画面 | 応答時間（3回） |
|---|---|
| ログイン | 265 / 242 / 295ms |
| 管理者ホーム | 200 / 203 / 179ms |
| 評価セット（参考配点つき） | 179 / 161 / 155ms |
| パスワードの変更 | 112 / 107 / 120ms |

- 静的ファイル（JS・CSS 計634KB・未圧縮換算）は brotli 圧縮 + 1年間のブラウザ保存（`public/_headers`）が効いており、2回目以降の読み込みは発生しない。

### 実測値（2026-08-11・サイドバー導入後の本番）

| 画面 | 応答時間（3回） | 画面の大きさ |
|---|---|---|
| ログイン | 120 / 54 / 94ms | 7KB |
| 管理者ホーム | 313 / 194 / 192ms | 18KB |
| 昇給の設定 | 162 / 145 / 221ms | 38KB |
| 評価セット | 181 / 196 / 191ms | 91KB |
| 評価基準の確認 | 290 / 272 / 210ms | 59KB |
| 制度マスタ | 384 / 357 / 412ms | 111KB |
| 等級要件 | 123 / 135 / 153ms | 27KB |
| 評価一覧 | 294 / 368 / 364ms | 23KB |

- サイドバー自体の重さは **1画面あたり約1.7KB**（全画面共通）。静的ファイルは11本・未圧縮640KB で、サイドバー導入前（634KB）とほぼ変わらない。
- 目立って重いのは制度マスタ（111KB）と評価セット（91KB）の2画面。原因は上の「参考データを最初から全部送っている」項目に集約した。
- 公開ファイルの容量は圧縮後 1,852KB（上限3,072KB）。

### 実測値（2026-08-11・v10 の本番・「開いたときだけ読む」導入後）

デプロイ後の本番URLでの実測（2周目。1周目は古いインスタンスが残るため参考にしない）。

| 画面 | 応答時間 | 画面の大きさ | 前回（v9 本番） |
|---|---|---|---|
| 管理者ホーム | 619ms（初回）/ 以降 100ms台 | 18KB | 18KB |
| **制度マスタ** | 292ms | **42KB** | 111KB |
| **評価セット** | 117ms | **46KB** | 91KB |
| 事業所KGIの達成率（新規） | 147ms | 32KB | — |
| 昇給の設定 | 125ms | 39KB | 38KB |
| 評価基準の確認 | 158ms | 59KB | 59KB |
| 等級要件 | 108ms | 27KB | 27KB |
| 評価一覧 | 212ms | 23KB | 23KB |
| 評価詳細 | 232ms | 69KB | — |

- 管理者ホームの初回 619ms は、しばらくアクセスが無かったあとの起動ぶん（2回目以降は100ms台）。
- 制度マスタの応答時間 384〜412ms → 292ms も改善したが、狙いどおり効いたのは大きさのほう（111KB → 42KB）。

- 折りたたみを開いたときに追加で読む量: ランク基準 6KB（8項目 / 40件）、元の配点表 4KB（1等級区分 50件）。
- 次に重いのは評価詳細（69KB）と評価基準の確認（59KB）。どちらも「中身を一覧して確かめる」ことが
  目的の画面なので、折りたたみにはしない。
- 公開ファイルの容量は圧縮後 1,981KB（上限3,072KB）。v9 の 1,852KB から 129KB 増えた（達成率の画面ぶん）。

---

## 正本（KPI基準定義）との突合で残った課題（2026-08-10）

`data/_authoritative-kpi-criteria.tsv` を正本として全件を実DBと突合した結果、埋められなかったものを残す。
突合の内訳は `docs/migration-mapping.md` §10 を参照。

| # | 残っていること | なぜ今できないか | 次にやること |
|---|---|---|---|
| ~~C1~~ | ~~**事業所KGI達成率の入力画面が無い**~~ **→ 対応済み（v10）** | `/admin/kgi` を追加。事業所×サイクルで達成率(%)を登録し、保存するとその事業所の**確認中の評価だけ**個人Pt・賞与額を計算し直す（確定済みは過去評価の不変性のため据え置き、据え置いた件数を画面に出す）。未登録は行を作らず（0%で保存しない）、個人Pt・賞与額は null のまま「達成率が未登録のため出せません」と表示。変更履歴は `office_kgi_revisions`。詳細は `docs/product/spec.md` §11-1 | 残りは C2（配点確定後に「仮」バッジを外す） |
| C2 | **賞与額・個人Ptは「仮」のまま** | 1点あたり3,200円・原資930,000円は元シートの値だが、配点そのものが未確定 | 配点が確定したら「仮」バッジを外す |
| ~~C3~~ | ~~**ランク→点数の換算方式が未決**~~ **→ 決定済み（2026-08-11 / v11）** | **「等級別配点 × ランク割合（A=100%/B=80%/C=60%/D=40%/E=0%）」に一本化**。項目別絶対点方式は新規に選べないようにした（APIでも拒否）。過去の確定済み評価は当時の方式のスナップショットのまま保持し、表示も当時の方式で行う | 残りは C2（配点確定後に「仮」バッジを外す） |
| C4 | **閾値の妥当性は検証できていない** | 実運用のデータが1件も無い。DBの48件はデモ用シードが作ったもので、実データではない | 最初の半期の評価が確定したら、実際の分布を見てランクが偏っていないか確認する |
| C5 | **No.4 昇給率の文言変更は次回アンケートから** | 正本【9】の指示どおり本人分を除く文言に直したが、すでに配布・回答済みのアンケートの文言は変えていない（過去評価の不変性を守るため） | 次にアンケートを作り直したときから新しい文言になる |
| ~~C7~~ | ~~**等級要件の画面まわりの仕上げ**~~ **→ 対応済み（2026-08-12）** | 等級要件・昇格要件は共通の版ライフサイクルと「今後使わない／もう一度使う」に統一。行動指針の等級割当も、上部一覧＋下部単一編集の二重構造をやめ、各等級の行/カード内で選択・保存する仕様へ統一した | 残課題は、実運用後に文言や到達性で迷いが残るかの観察だけ |
| ~~C8~~ | ~~**評価詳細画面に個人Pt・賞与額をまだ出していない**~~ **→ 対応済み（v10）** | 評価詳細に「個人Pt と 賞与額（仮）」を追加。達成率・係数・`bonus_rationale` を併記し、賞与額には「仮」バッジを付けた。未登録のときは金額を出さず、`/admin/kgi` への導線つきで理由を出す。**評価される側には出さない**（個人Pt ÷ 係数で、隠しているKPI評価点合計が逆算できるため、APIレスポンスの段階で落としている） | C2（配点確定後に「仮」を外す） |
| C6 | **昇給額の自動計算（連続達成の倍率・事業所調整率・端数処理）が未実装** | 設定値はDBに入っている（`raise_policies` / `raise_settings` / `offices.raise_adjust_rate`）が、実際に金額を出す処理がまだ無い | 昇給額 ＝ 等級別の基本額 × 事業所の調整率 × 連続達成の倍率 を実装し、端数処理の単位で切り上げる |

---

## エレガント化改善（2026-08-11 / v11）で残したもの

このときの改善で、**やった上で残した**もの・**あえて着手しなかった**ものを残す。
（何を直したかは `docs/product/spec.md` §2-1・§10 と、この日のPRを参照）

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| E1 | **昇給ルールの画面に「8項目中8項目」と出たままになっている** | 実際の昇給判定は「**その人が実際に評価された項目が全部A**」で動いており、等級区分ごとの項目数（1／3／6／7／8）に自動で追随する（`raise_policies` の数値は判定に使っていない）。ただし `/admin/raises` の表示だけが元シートの「8項目中8項目」のまま残っており、Beginner の人を見ている管理者には話が合わない。**あわせて、Beginner は1項目Aで昇給・Manager は8項目すべてAとなり難易度が等級で揃わない**が、これは制度の決めごとなのでこちらでは決めない | ①`/admin/raises` の表示を等級区分ごとに出し分ける ②等級区分ごとに必要なA件数を変えるかどうかを依頼者に決めてもらう |
| E2 | **催促文のURLが、開いている画面のアドレスに依存している** | 回答一覧の「未回答の方への連絡文をコピーする」が作るURLは、`APP_ORIGIN` を設定すればそちらを使うようにしたが、設定自体はまだ入れていない（設定ファイルの変更はデプロイの手順に関わるため分けた） | `wrangler.jsonc` の `vars` に `APP_ORIGIN` を追加する |
| E3 | **回答期限の個別延長は管理者画面にしかない** | 延長のAPIは上長以上に開いているが、付与する画面は管理者の回答一覧だけにある。上長が自分の部下に延長を出す運用が要るかどうかは、締切を実際に効かせてみないと分からない | 運用してみて必要なら `/manager` 配下に同じ画面を足す |
| E4 | **回答期限は日付単位（日本時間）まで。時刻は指定できない** | 「9月30日まで」を当日いっぱいと読む前提に合わせた。時刻まで刻むと、締切間際の1件が何時何分で切られたかの説明が要る | 時刻指定の要望が出てから |
| E5 | **アンケートの版番号を同時に採番したときの取り合いが未対処** | 同じ評価期間・同じ等級の下書きを2人が同時に作ると、版番号が重なって保存に失敗しうる。実データ63件に衝突の跡は無く、管理者は各社1〜2人のため実害が出ていない | 保存に失敗したときに版番号を1回引き直して再試行する（半日以内） |
| E6 | **過去の確定済み評価（本番48件）には、本人向けの判定理由が保存されていない** | 判定理由を「評価者向け」「本人向け」の2本立てにしたのは今回から。過去の分を作り直すと、確定済みの評価を書き換えることになる（過去評価の不変性に反する） | 作り直さない。本人が過去の評価を開いたときは、ランク・実績値・項目名という安全な値から説明文をその場で組み立てて出している（その旨も画面に明記） |
| E7 | **利用者・権限操作の監査証跡** | 今回は role/company/grade/manager の不変条件、停止会社の遮断、パスワード再発行時のセッション失効を先に直し、監査テーブルの追加までは同じ変更に含めなかった。制度マスタの監査とは別に、SUPER_ADMIN 付与・所属会社変更・利用停止/再開・パスワード再発行はアクセス制御そのものなので、誰が実行したかを後から説明できる必要がある | `account_security_revisions`（仮称）を追加し、actor、target、action、変更前後、実行時刻、理由を、対象の更新と同じ原子的な保存単位で記録する。パスワード本文・hash・セッションtokenは絶対に残さない |
| E8 | **認証済み画面を通るマイグレーション・スモーク** | 現行のデプロイ後スモークは `/` と `/login` が中心で、ログイン後に初めて読む `profile_field_policies` のマイグレーション漏れを検知できない。認証済みスモークにはロール別の検証アカウントと安全なsecret運用が必要なため、今回のコード変更とは分けた | staging で `wrangler d1 migrations list` と `0010_profile_field_policies.sql` の適用済み確認を行い、migration→deploy の順で `/account`、`/admin/members`、`/system/users` を EMPLOYEE / COMPANY_ADMIN / SUPER_ADMIN の各セッションで確認する。`no such table`、403の誤判定、他社データ混入を失敗条件にする |

## 運用メモ

- 次に着手する項目は、実際に触ってみた上で選んでください（優先順位は依頼側で決めるものです）。
- 2026-08-11 に見直した結果、次の4件は**あえて着手していません**。理由も残します。
  - ~~制度マスタ全般の監査ログ — 全マスターのイベントストア化はせず、運用上必要になった対象から追加する~~ **2026-08-12 に着手・完了**（詳細は「制度マスタにイベントストアを足した回」参照）。実際に書き込みが起きている12種のマスタに `constitution_events`（誰が・いつ・何を・変更前後）を記録するようにした
  - **参考配点の等級区分の自動判定** — 評価セットと等級の対応が1対1でない会社があり、自動で選ぶと違う等級の点数を入れてしまう恐れがあるため。いまは人が選ぶ形のまま
  - **一覧のページング** — 1社あたり数十人規模で、実測でも一覧は23〜30KB・数十msに収まっているため。数百人規模になったら
  - **未回答者の可視化** — すでに実装済みでした（回答一覧に在籍者を全員並べ、未回答バッジと件数を表示）。残っているのは催促メールの自動送信だけで、こちらはメール基盤が要ります
- テストが読むファイルのうち、リポジトリで追跡しているのは評価基準の正本（`data/_authoritative-kpi-criteria.tsv`）だけです。他にリポジトリ外のファイルに依存しているテストは無く、新しく取得し直した状態でもテストは全件通ります。初期データ投入スクリプト（`scripts/seed-data.mjs` ほか）だけは `data/*.json`（個人情報を含むため追跡しない）を必要とするため、投入をやり直すときは元の抽出ファイルを手元に置いてから実行してください。
- 追加開発は1機能ずつ行い、既存の流れを壊していないことを確認してから公開します。

---

## 別の等級のアンケートも読めるようにした件（2026-08-12）

上位の権限者が下位のアンケートの中身を確認できるようにした。回答用のURLを対象外の人が開いても閉ざさず、`/forms/[id]`（確認専用）へ送る。回答は等級が一致する人だけができ、保存APIでも弾く。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| F1 | **他の等級のアンケートは、確認専用画面から探す形のまま** | `/me/forms`（実績を報告する）に他等級のアンケートを並べると「自分が答えるもの」と混ざり、回答漏れの原因になる。他等級は「アンケートの中身」の一覧から探す | 他等級を見に行く頻度が高いようなら、`/forms` の一覧に等級での絞り込みを足す |
| F2 | **確認専用画面には、回答状況（誰が出したか）を出していない** | 回答データの閲覧権限は COMPANY_ADMIN 以上のまま。中身が読めることと回答が読めることは別扱いにしている | 上長が自分の部下の提出状況を見る導線は `/manager` 側で足す |
| F3 | **「別の等級の方向けです」の案内に、対象等級の人数や配布状況は出していない** | 中身の確認に必要な情報ではなく、画面の目的（設問を読む）から外れるため | 管理者から要望が出てから |

---

## 制度設定の項目を消せるようにした回（2026-08-12）で残したもの

「使わない」しか無く、試しに作った項目が一覧に残り続ける問題への対応。**やった上で残した**ことを記録する。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| D1 | **等級・事業所・利用者・アンケートには「完全に消す」を付けていない** | いずれも過去の評価が必ずぶら下がる（評価1件は必ず等級・利用者・アンケートを指す）。消せる条件がほぼ成立しないうえ、成立するときだけ消せる作りは「消せたり消せなかったり」して分かりにくい。利用停止・締め切りで足りている | 「作ったばかりで一度も使っていない等級を消したい」という具体的な場面が出てから、同じ判定の仕組みを流用して足す |
| D2 | **消した記録（誰がいつ何を消したか）を残していない** | 消せるのは「一度も使っていないもの」だけなので、消えても業務の記録は1文字も変わらない。監査テーブルの追加は E7（利用者・権限操作の監査証跡）とまとめて作るのが素直 | E7 と同時に、制度設定の削除も同じ台帳に載せる |
| D3 | **消したものを元に戻す手段は無い** | 完全に消す操作なので、確認の文章で「元に戻せません」「何が一緒に消えるか」を示すところまでにした。ゴミ箱を作ると「消したのに残っている」状態が生まれ、今回の要望（一覧に残り続けるのをやめたい）と逆になる | 誤って消した申告が出たら、その時点でゴミ箱ではなく作り直しの導線を検討する |
| D4 | **KPI・評価セットの項目には触れていない** | 同じ時期に別の担当が `/admin/scheme` を作り直しており、二重に直すと片方が消える | 作り直しが反映されたあとで、同じ「使わない／完全に消す」の作法に揃えるか点検する |
| D5 | **「使わない」の見た目は、面の灰色・破線の枠・色を落とす・札の4点で示している** | 色だけに頼らないようにしつつ、本文の文字は薄くしていない（薄くすると読みやすさの下限を割る）。定義は `src/app/globals.css` の1箇所と、共通部品の `off` / `rowOff` だけ | 文字サイズの見直しをする担当がこの1箇所を触るときは、面・破線・grayscale を消さないこと |
| D6 | **見た目の確認を機械だけに任せない運用がまだ言葉のまま** | 2026-08-12、テストは609件通るのに「使わない」の見た目が人の目で見分けられない状態で出してしまった（面の色の差が2%しかなかった）。いまは `docs/product/spec.md` §7-7 に「見た目の変更は build → preview で目視確認」と書き、取りこぼしは `src/components/off-state.test.ts` で検出している | 画面の見た目を変える作業では、変更前後の画面を並べて残す（撮って比べる）ところまでを手順に入れる |

### 等級要件・昇格要件の版管理で解消したこと

- **「今後使わない」は可逆**: 停止中の現行版を同じidのまま「もう一度使う」に戻す。完全削除とは区別した。
- **「内容を直す」は上書きしない**: 必ず新idを作り、`previous_version_id` で直前の現行版へつなぐ。過去版・作成済みフォーム・評価は変えない。
- **過去版をそのまま再開しない**: 過去の内容を使いたい場合は、現行版の後続となる新版を作る。
- **将来構成だけは更新できる**: `seq` / 現行版の `is_active` は次に作るフォームの選択・順序として更新できる。
- **stale通知を正した**: フォームへ写す等級要件・昇格要件は、再集計しても既存評価が変わらないため監視対象から外す。
- **過剰な全マスター共通化は見送った**: 今回の共通化は同じ性質の等級要件・昇格要件だけ。横断監査は上記セキュリティ残課題として分離する。

残る観察項目は、10件上限時の再開理由、折り畳んだ停止項目への到達性、過去版から新版を作る操作の理解度である。仕様とサーバー検査は実装対象に含め、運用で迷いが確認された場合だけ文言・配置を追加調整する。

---

## 回ごとの記録（分離）

500行上限のため、以降の回ごとの残課題メモは [`docs/product/backlog-session-notes.md`](./backlog-session-notes.md) に移した。

## 版として残す仕組みの試験を足した回（2026-08-12）で残したもの

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| U1 | **系譜の読み方が2箇所にある** | 画面用の `classifyVersionedItems`（`src/components/VersionedMasterSections.tsx`）と、計算用の `currentVersionRows`（`src/lib/domain/versioned-master.ts`）が同じ判定を別々に書いている。いまは答えが一致するが、片方だけ直すと画面と計算が食い違う。網羅率の100%が掛かるのは計算側だけなので、画面側は静かにずれる | 画面側を計算側の呼び出しに寄せる。今回は PR の作りを変えない方針のため見送った |
| U2 | **使われていない関数が2つある** | `historicalOf`（等級要件）と `isCurrentVersion`（系譜）は、どこからも呼ばれていない。画面は上記の別実装を使っている。消すか使うかは、この版の作者の判断 | 画面側を寄せる作業のときに、どちらかへ統一する |
| U3 | **昇格要件の追加だけ、データベースの検査の訳が無い** | 等級要件の追加は検査の失敗を日本語へ訳しているが、昇格要件の追加（`promotionRequirementCreate`）だけ訳していない。上限が無い項目なので今は当たらないが、当たると英語のまま画面に出る | 上限や検査を足すときに、同じ訳を通す |

---

## 制度マスタにイベントストアを足した回（2026-08-12）で残したもの

制度マスタ（等級・要件・行動指針・昇給・事業所・ランク基準・KGI係数）の変更を、上書きではなく
「誰が・いつ・どの項目を・変更前後どう変えたか」を1件ずつ積み重ねる記録（`constitution_events`）として残すようにした。
現在の内容は記録を古い順に重ねて再現できる（再生できる）。差分（変わった列だけ）を持つため、変えていない列は記録が増えない。

**やったこと**:
- `constitution_events` テーブル（`drizzle/migrations/0014_constitution_events.sql`）と、記録・再生・一覧を行う
  `src/lib/domain/constitution-events.ts`（`recordConstitutionEvent` / `replayConstitutionEntity` / `listConstitutionEvents`）を用意した。
- 実際に書き込みが起きている4つの入口すべてに記録を差し込んだ:
  `apply-master-update.ts`（等級・昇給・事業所・ランク基準・KGI係数など8種）、
  `versioned-requirement-update.ts`（等級要件・昇格要件の作成・改訂・使う/使わない・並び替え）、
  `apply-behavior-master-update.ts`（行動指針の基準・観点・5段階の文章）、
  `delete-master-item.ts`（完全に消す操作）。
- 記録した人（`actorId`）は、その操作をした人のIDをそのまま使う。
- 新規テスト `src/lib/domain/constitution-events.test.ts`（4件）に加え、既存テストを含めて全1319件が通ることを確認した。
  ローカルD1へのマイグレーション適用（0000〜0014）と、Workersランタイムでのpreview起動（ログイン・管理画面・権限チェック）も確認した。

**等級要件・昇格要件の「版」の仕組み（`previous_version_id` の連鎖）とは別物として残した**:
`previous_version_id` の連鎖は、DBの検査（トリガー）で「過去版は書き換えない」「使える現行版は同時に1件まで」
「過去版をそのまま再開しない」を強制する仕組みで、画面の系譜表示（`versionFamilyIds` など）もこれを使っている。
今回のイベント記録はこの上に乗せた別責務（誰が変えたかの記録）であり、連鎖の仕組みをイベント記録に置き換える
（再生だけで版の制約まで再現する）のは、検査をやり直すリスクが大きく、今回困っていた「誰が変えたか分からない」
という問題の解決には不要だったため、**2つを並存させる判断にした**。系譜をまとめて1つの記録対象として扱うため、
版の連鎖をたどって根っこのID（`lineageRootId`）を記録対象のIDとして使っている。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| V1 | **記録の対象は「実際に書き込みが起きている12種」だけ** | KPI項目・評価セット・昇給パターンなど（`kpiCategories`／`evaluationSchemes`／`raisePatterns`ほか）は、現時点で専用の変更APIが無く上書き先が無いため対象にしていない | それぞれに変更APIができたタイミングで、同じ記録を差し込む |
| V2 | **記録を見る画面が無い** | `listConstitutionEvents` は用意したが、一覧・検索の管理画面はまだ無い。今回は「記録を残す」までが範囲 | 「いつ誰が変えたか知りたい」という要望が出たら、対象を絞った履歴画面を1つ足す |
| V3 | **利用者・権限操作の監査は含めていない** | 制度マスタとは別責務（E7と同じ対象）。今回は制度マスタの4つの入口だけに絞った | E7に着手するときに、同じ考え方（差分・actor・再生可能）を使うかを検討する |
