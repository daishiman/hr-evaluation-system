# 残課題 — 回ごとの記録

親文書: [`docs/product/backlog.md`](./backlog.md)

アクティブな残課題は親文書に残し、完了した回の詳細メモだけをここに置く。

## 数字の欄を「整数だけ」にできるようにした件 / ランクの境界の矛盾を防いだ件（2026-08-12）

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| G1 | **パソコンでは小数点キー自体は打てる**（打った後に理由を出して断る） | キーを握り潰すと「3.5」と打った人に黙って「35」が保存されうる（10倍の誤り）。打った値と保存される値が食い違うほうが危険 | スマートフォンでは小数点キーが最初から出ないようにしてある。パソコン側は現状のままでよいか、運用して様子を見る |
| G2 | **ランク基準の行を増やす・減らす画面が無い**（A〜Eの5段が前提） | 段数を変えると配点・集計の前提が動く。今回の依頼（重なり・隙間の防止）とは別の話 | 5段以外を使いたい会社が出てきたら |
| G3 | **既存の設問の「整数だけ」の印は、制度マスタの文言から一度だけ決めた** | 設問の説明文は判定に使えなかった（本番の説明文に「整数」の語が無く、1008件中0件しか当たらない）。制度マスタ側の文言（`kpi_questions.validation`）を正として1008件に印を付けた | 個別に変えたい設問は、アンケートの編集画面のチェックボックスで切り替えられる |

- **確認済み（本番データ、読むだけ）**: 数値の回答1806件のうち小数を含むもの0件（勝手に丸めた・消したものは無い）。KPI項目132件・ランク基準660行を新しい検査にかけて、重なり・隙間・端が閉じている・繋ぎ目が空欄はいずれも0件（既存の設定が編集できなくなる会社は無い）。

---

## 評価の計算・保存・表示を、境界値つきのテストで固めた件（2026-08-12）

人事評価は、数字が1点でも狂えば人の昇給・昇格が変わる。「計算は合っているが保存されていない」「保存されているが画面に出ていない」を単体テストだけでは見つけられないため、**本物のデータベース（メモリ上のSQLiteに本番と同じマイグレーションを適用したもの）に対して、集計→保存→読み出し→表示までを通す結合テスト**を追加し、あわせて境界値の単体テストを足した。

- テスト件数: 590件 → **991件**（既存はすべて通り続けている）
- 網羅率（`pnpm run test:coverage`）: 行 **100%** / 関数 **100%** / 文 99.73% / 分岐 99.27%
- 網羅率が下がったら CI が赤くなるようにした（`vitest.config.mts` の `thresholds`、`.github/workflows/ci.yml` の「テスト（網羅率の下限つき）」）

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| H1 | **実績値の丸めで、ちょうど「小数第3位が5」の値が下に寄る** | 1.005 のような値はコンピュータ内部で5より僅かに小さく持たれるため、四捨五入すると 1.00 になる。**直すと過去の実績値が変わりうる**ため、独断では直さず現状を試験として記録した（`src/lib/domain/formula.boundary.test.ts`） | 影響はランク境界と 0.005 未満しか違わない場合に限られる。直すかどうかは、確定済み評価への影響を確認したうえで判断する |
| H2 | **画面部品（React コンポーネント）のテストは無い** | 表示の一致は「保存された値と、画面へ渡す関数（`scopeEvaluationRow` / `scopeEvaluationItem` / `buildRadarValues`）の出力が一致すること」で固定した。描画そのものの試験には別の道具立てが要る | 画面の崩れを見たいなら、ブラウザを使った試験を別途入れる |
| H3 | **到達不能な保険が7か所ある（100%にならない残り）** | 型・データベースの参照制約・定数によって実際には通らない分岐。テストが書けていないのではなく、書けない | `evaluate.ts` 281/304/490、`impact.ts` 61、`evaluation-view.ts` 293、`form-question-text.ts` 115、`initial-password.ts` 60。将来この前提が変わったら試験を足す |
| H4 | **APIの受け口（Route Handler）は結合テストの対象外** | 権限の判定は既存の `evaluation-authority` の試験で固めてある。受け口まで含めると認証の作り込みが要る | 権限まわりを触るときに、受け口ごとの試験を足す |

---

## 「使わない」の見た目が実際には見えていなかった件を直した回（2026-08-12）で残したもの

観点を「使わない」にしてもカードの見た目が使用中と同じだった件の作り直し。面の灰色を濃くし、破線・色落ち（grayscale）・札を足し、取りこぼしを `src/components/off-state.test.ts` で機械検出できるようにした。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| I1 | **等級・事業所には「使わない」の見た目を持たせていない** | どちらもデータ上は使用中／使用しないの欄を持つが、画面のどこにも切り替える操作が無い（到達できない状態）。到達できない見た目を足すと、灰色になったのに戻す手段が無い画面が生まれる | 切り替えを足したら見た目も足す。忘れないよう `off-state.test.ts` に「等級・事業所の切り替えを送る画面ができたら落ちる」検査を入れてある |
| I2 | **評価期間を選ぶタブは、締め切り済みでも沈めていない** | タブは「どれを見るか」を選ぶ道具で、締め切り済みの期間を見に行くのは通常の操作。ここを灰色にすると「選んではいけない」と読めてしまう | 一覧の中身（アンケート・評価期間のカード）側は沈めてあるので、いまは分けたままでよい |
| I3 | **見た目の比較は手作業（画面を撮って並べる）のまま** | 画像を機械で比べる仕組みは、文字サイズの見直しなど正当な変更でも落ちるため、いまの人数では手間のほうが大きい | 画面の作り直しが続くようなら、主要画面だけ撮って比べる仕組みを検討する |

- **確認済み（preview で目視）**: 使用中のカードは白＋実線、使わない設定のカードは灰色（#e4e7eb）＋破線＋色落ち＋「使用しない」の札。本文の文字色は変えていない。行動指針の観点／基準セット、等級要件、昇格要件、利用者（一覧・詳細）、アンケート（一覧・詳細）、評価期間、会社（システム管理）で同じ見た目になることを確認した。

---

## 計算まわりを網羅率100%にした回（2026-08-12）

「行100%・関数100%だが文と分岐に残りがある」状態をやめ、**計算に関わる15ファイルを4指標すべて100%** にした。以前 H3 に挙げていた「到達しない保険7か所」は、**実際に通るかどうかを1件ずつ試して**決着させた。

- テスト件数: 991件 → **1059件**
- 網羅率: **行・関数・文・分岐 すべて100%**（計算まわりはファイル単位で100%を必須にし、`vitest.config.mts` に下限を明記）
- 本番コードの変更: **判定・計算のルールは1つも変えていない**。通らない道（読む人を惑わせる保険）を外し、代わりにその前提を試験で固定した

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| J1 | **「通らないと思っていたが実は通る」道が1つ見つかった** | 括弧が極端に深い計算式は、計算式の不備とは別の形で止まる。既定の理由で判定外になることを試験で固定した（`src/lib/evaluate.failure-paths.test.ts`）。**評価の結果は変えていない** | 計算式の長さ・深さに上限を設けるかは、設定画面を触るときに合わせて検討 |
| J2 | **実績値が「無限大」になりうる** | 400桁のような極端な数や、極端な掛け算の結果は内部で無限大になる。提出時の検査（`checkAnswerNumbers`）で弾いているが、既存データの取り込み経路までは確かめていない | 取り込み（CSV）まわりを触るときに、同じ検査を通す |
| J3 | **画面部品（React コンポーネント）のテストは無い**（H2 から継続） | 表示の一致は「保存値と画面へ渡す関数の出力の一致」で固定している | 画面の崩れを見たいなら、ブラウザを使った試験を別途入れる |
| J4 | **APIの受け口（Route Handler）は結合テストの対象外**（H4 から継続） | 権限の判定は `evaluation-authority` の試験で固めてある | 権限まわりを触るときに、受け口ごとの試験を足す |

**H3 は解消済み**（7か所すべて決着）。H1（丸めの件）は依頼者の判断で**現状のまま**とし、試験で固定してある。
| D5 | **「使わない」の見た目は、面の色・破線の枠・札の3点で示している** | 色だけに頼らないようにしつつ、本文の文字は薄くしていない（薄くすると読みやすさの下限を割る）。定義は `src/app/globals.css` の1箇所と `src/components/ui.tsx` の `off` だけ | 文字サイズの見直しをする担当がこの1箇所を触るときは、面と破線を消さないこと |

---


## 文字の大きさと固定表示（2026-08-12）で残したもの

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| K1 | **利用者が自分で文字の大きさを変えられない** | 今回は全体の下限を引き上げて「小さい」を解消することを優先した。切り替えを付けると、段の組み合わせごとに折返し・固定帯の高さ・入力欄の並びを全画面で検証し直すことになり、範囲が一気に広がる。まず一段大きくした状態を使ってもらい、なお合わないかどうかを見てから決めたい | ブラウザ側の拡大（⌘+／Ctrl+）で足りるかをまず確認する。足りなければ「標準／大きめ」の2段だけを設定に置き、`@theme` の値を丸ごと差し替える形で入れる（画面ごとの個別対応はしない） |
| K2 | ~~**カードの頭の固定は、狭い画面（〜639px）では効かせていない**~~ **→ 2026-08-12 解消（回答画面のみ）** | 当初：375×700 の実測で、上の固定物が約195px・下の操作バーが約116px を使い、帯（約93px）を足すと入力欄に残る高さが画面の半分を切るため見送っていた | 依頼者から「スマホで答える人がいる」と確認できたため、**回答画面だけ**帯を1行（35px）に縮めて固定を戻した。実測で入力欄に残る高さは 352px（375×700 で50%）と半分を下回らない。組み立て画面（管理者用）は固定しないまま。詳細は `docs/product/spec.md` §16-1 |
| K3 | **固定していないカードが残っている**（アンケートの組み立て・回答、一覧や履歴のカード） | 1問1カードで短く、参照し続ける値は上の見出しの帯にあるため。固定を増やすほど画面が狭くなる | 「上に戻って確かめた」という声が出た画面だけ、その都度 `CardHead` の `pinned` を付ける（帯に載せるのは打ちながら参照する値だけ、を守る） |

---

## 見本用のサンプルデータを入れた件（2026-08-12）

過去の評価が画面でどう見えるかを確認するため、会社「見本商事（サンプル）」を1社追加し、確定済み評価40件（10名×4期）を入れた。既存の会社・利用者・アンケートには一切触れていない（詳細は `docs/product/spec.md` §14）。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| L1 | ~~**本人向けの「昇格できない理由」が、保存した文ではなく共通の言い換え文になる**~~ **→ 解消済み（2026-08-12 / 詳細は `docs/product/spec.md` §19）** | 判定側（`src/lib/domain/scoring.ts`）が作る本人向けの文に「昇格に必要な水準」という言い回しが入っており、本人に基準を出さないための検査（`containsCriteriaLeak`）が同じ言葉を弾くため、保存した文が使われず共通文に置き換わる。**本物の評価でも同じことが起きており、サンプル特有の問題ではない**。表示自体は意味が通っているため、リリースを止める不具合ではないと判断した | 判定側の文言を「昇格に必要」を使わない言い方に直す（例：「昇格の目安にまだ届いていません」）。過去の確定済み評価は書き換えない |
| L2 | **サンプル会社は、本番でも他の会社と同じ一覧に並ぶ** | 会社名に「（サンプル）」を付けて見分けられるようにするところまでにした。一覧から隠す仕組み（サンプル用のフラグ）を足すとテーブル変更が要るうえ、隠すと逆に消し忘れる | 本番運用が始まったら消す（`pnpm run db:sample:remove:remote`）。運用開始後も残すなら、そのときにフラグを検討する |
| L3 | **回答前（受付中）の画面は、サンプルでは見本にならない** | 5期すべてを締め済みにした。受付中の期を1つでも開けておくと、本番の「未回答の方」一覧に見本の12名が並び、**本物の未回答者が埋もれる**ため、依頼者の判断でそちらを優先した。2026年度上期は「締め切ったが評価がまだ無い期」として残してある（`withResults: false`） | 回答前の画面を見たくなったら、そのときに評価期間の画面から開けば済む |
| L4 | **サンプルの利用者でログインするには、毎回仮パスワードを発行する必要がある** | 平文パスワードを保存・出力しない原則を優先した（決まった値を配ると本番アカウントと同じ手口で入られる余地ができる） | このままにする。ログインしたいときは管理画面の「仮パスワードを再発行する」から発行する |

---

## 確定されないまま放置された評価に気づける仕組みを入れた回（2026-08-12）

締め切った評価期間に残っている「確定されていない評価」を、各ロールのホームの最上部に出すようにした。
原因は件数の出し忘れではなく、**どの画面も「いま開いている期間」しか見ていなかった**こと（締め切った瞬間に全画面から消えていた）。
判定・並べ替え・文言は `src/lib/domain/stalled-evaluations.ts`（純関数・網羅率100%）、DBの読みと会社の絞り込みは `src/lib/stalled.ts`、画面は `src/components/StalledEvaluationsNotice.tsx`。**読むだけで、確定・差し戻し・削除・再集計は一切しない**（詳細は `docs/product/spec.md` §17）。

- 本番の実データを確認: 評価88件はすべて確定済み、提出済み回答88件もすべて評価済みで、**いま放置されているものは0件**。手元で「締め切ったのに未確定」の状態を作って動きを確かめた。
- テスト件数: 1059件 → **1097件**（新規38件）。網羅率は行・関数・文・分岐すべて100%を維持。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| N1 | **知らせは、その人がホームを開いたときにしか出ない** | メールやチャットへの通知を足すには送信基盤と「誰に何通まで送るか」の設計が要る。まず「開けば必ず目に入る」状態を作り、それでも放置が残るかを見たい | 放置が実際に残るようなら、`cloudflare-email-service` で週1回のまとめ送信を検討する（1件ごとに送ると読まれなくなる） |
| N2 | **14日・30日という線に、実績の裏付けがない** | 本番の確定済み88件は移行時に一括投入されており、`finalized_at` が全件同じ日。締め切りから確定までに実際どれだけかかっているかを測れない | 半期を1回まわしたら、実際の所要日数を数えて線を引き直す。変えるのは `OVERDUE_DAYS` / `LONG_DAYS` の2定数だけ |
| N3 | **一般（EMPLOYEE）には何も出していない** | 「あなたの評価が放置されています」を本人に見せると、本人からは何もできないまま不安だけが残る。まず確定できる人に届けることを優先した | 本人向けに出すなら「確定待ちです」という事実だけにし、催促の導線は付けない |
| N4 | **全体管理者の画面は、会社数が増えると1社ずつ数える作りのまま** | いまは会社4社なので、1社ずつ読んでも体感できる差が出ない。1本のSQLにまとめると会社の絞り込みが1箇所に収まらなくなり、他社の行が混ざる事故の余地が増える | 会社が20社を超えたら、会社の絞り込みを保ったまま件数だけを1本にまとめる |
| N5 | **「締め切ったのに未確定」を作らないための予防はしていない** | 期間を締め切るときに未確定が残っていても止めていない（止めると、締め切れずに運用が詰まる）。今回は起きたあとに気づける形にした | 締め切る操作の確認の窓に「未確定が◯件残っています」と出すのが次の一手。締め切り自体は止めない |

### 同じ回の続き：締め切る前の予防（N5 を実装）と、文字を大きくした副作用の手当て（2026-08-12）

**N5 は同じ回にまとめて入れた。** 別々に出すと、放置に気づく仕組みだけが先に本番へ出て、
放置を作らない側が1回ぶん遅れる。予防と発見はひと組で初めて意味を持つため、同じ反映に含めた。

- 「受付を締め切る」を押したときの確認の窓に、**未確定の件数と、残っている方の名前（3人まで、以降は人数）**を出すようにした。
  **締め切り自体は止めない**（期末に締められないほうが業務は困る）。**0件のときはこれまでどおりの確認文だけ**で、手間は増やさない。
- 窓を開かなくても分かるよう、期間の見出しに「未確定 ◯件」を添え、「残っている方を見る」から一覧へ飛べるようにした。
- 文言は `cycleCloseConfirmText`（`src/lib/domain/stalled-evaluations.ts`・網羅率100%）、
  誰が残っているかの読み出しは `listUnfinalizedNamesInCycle`（`src/lib/stalled.ts`・会社の絞り込みはサーバー側）。

あわせて、依頼者から指摘された「操作する会社名の下が欠ける」「灰色の帯」を直した（詳細は `docs/product/spec.md` §18）。

- テスト件数: 1097件 → **1138件**。網羅率は文・分岐・関数・行すべて100%を維持。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| N6 | **未確定の件数は「受付中の期間」ごとに1回ずつ数えている** | 受付中の期間はふつう1つなので、いまは1回で済む。まとめて数える書き方にすると、会社の絞り込みが1箇所に収まらなくなる | 受付中の期間を同時に何本も持つ運用が始まったら、1本にまとめる |
| N7 | **確認の窓に出す名前は3人まで** | 全員を並べると、肝心の「よろしいですか」が画面の外に出る | 全員を見たい場面が出てきたら、窓ではなく「残っている方を見る」の一覧を厚くする |
| N8 | **入力欄の高さの決め打ちは検査で止めたが、他の部品（札・ボタン）の高さは見ていない** | 札とボタンは `min-height`（下限）で書かれており、文字が大きくなれば伸びる作りになっている。いま欠けている箇所は無かった | 固定の高さを新しく書きたくなったら、そのとき検査の対象を広げる |
| N9 | **伸びる棒（進捗バー）の色は1色だけ** | もともと色分けの修飾クラス（`is-brand` / `is-base`）が用意されていたが、どこからも使われていなかった。使われていない分岐を残すより、既定色を1つ置くほうが崩れない | 「達成/未達成」で色を変えたくなったら、そのとき修飾クラスを使い直す（既定色はそのまま残す） |

---

## 外から入る数値の検査をそろえた件（2026-08-12）

「数値がアプリの外から入ってくる経路」を洗い出し、貼り付け取り込みだけが提出時より緩かった（J2）のを直した。あわせて、括弧が極端に深い計算式が理由の分からない形で落ちていた件（J1）を、読んで直せる言葉で断るようにした。**J1・J2 はこれで解消**（詳細は `docs/product/spec.md` §15）。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| P1 | **アンケートの設問を作るときの「下限・上限」自体には、いまも上限がない** | 受け口（`/api/forms/[id]/questions`）は下限・上限を無制限で受け付ける（`max < min` だけを断る）。ここに極端な値を置いても、回答の側で 1兆の上限に当たるため、極端な値が保存される道はふさがっている。設問を作れるのは会社管理者だけで、いまの登録内容は上限 1〜130 の範囲に収まっている | 設問づくりの画面を触るときに、下限・上限にも同じ 1兆の決まりを当てる |
| P2 | **ランク基準の下限・上限も、受け口では無制限** | 重なり・隙間の検査（`checkRankBoundaries`）が実質の防波堤になっており、極端な値を1つ置くと「隣のランクとつながらない」で断られる | 制度設定まわりを触るときに、下限・上限にも 1兆の決まりを当てる |
| P3 | **指数表記（`1.2E+15`）のセルは受け付けなくなった** | 表計算が大きな数を勝手にこの形で書き出すことがある。ただし、そこまで大きい数はどのみち 1兆の上限を超える。黙って別の数として保存するより、理由を出して断るほうが安全と判断した（本番の回答は最大 5515 で、この形の値は1件も無い） | 実際に困ったという声が出たら、指数表記だけを読み取る扱いを足す（そのとき提出時の検査にも同じものを入れる） |
| P4 | **社員名簿の取り込みには数値の項目が無い** | 氏名・メール・等級・事業所・入社日など、すべて文字と日付だけだった。検査するものが無い | 名簿に数値の項目（給与など）を足すときは、同じ `parseImportedNumber` を通すこと |

---

## カードの頭の固定をアンケートの2画面へ広げた件（2026-08-12）

**K3 は解消済み**（アンケートの回答・組み立てに適用。一覧・履歴は「打つ欄が無い」ため意図的に入れない）。
判定の基準と実測は `docs/product/spec.md` §16。
**K2（狭い画面では固定しない）は、回答画面だけ例外にして解消した**（依頼者に「スマホで答える人がいる」と確認したため）。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| M1 | **帯の下に、いま打っている欄の見出しが隠れることがある** | 画面の途中で止まると、固定した帯が直下の設問名に重なる。キーボードで移動したときは `scroll-padding-top` が働いて帯の下に潜らないため、実害が出るのは「マウスで少しだけスクロールした」ときに限られる。避けるには帯を透けさせるか本文に余白を足すことになり、どちらも別の読みにくさを生む | 「打っている欄の名前が見えない」という声が出たら、帯の高さぶんの余白を設問の間に足すかを検討する（1箇所の共通指定で行う） |
| M2 | ~~**狭い画面（〜639px）では、回答画面でも帯が固定されない**~~ **→ 同じ回の中で解消** | 依頼者に確認したところ「スマホで答える人がいる」と分かったため、帯を1行（35px）に縮めて固定を戻した。答え方の決まりは消さずカード本文の先頭へ移してある | **375×700 では基準ぎりぎり（残り352px＝50%、余裕2px）。この画面で固定物をこれ以上増やさないこと。**増やすときは必ず実測し直す（`docs/product/spec.md` §16-1） |
| M3 | **帯の見え方はブラウザでの実測1回ぶんしか確かめていない** | Chrome の 1280×800／375×700／390×844 で測った。Safari・Firefox や、文字を拡大した状態での組み合わせまでは通していない。固定の指定は共通部品1箇所なので、崩れるとしても全画面同時に崩れる（画面ごとにバラバラにはならない） | 画面の見た目を機械で見張る仕組みを入れるとき（J3）に、この2画面を対象に含める |
| M4 | **文字盤（スマートフォンのキーボード）が出ているかは、見える高さの減り方から推し量っている** | 「いま文字盤が出ている」を直接教えてくれる仕組みはブラウザに無い。見える高さが1/4以上減ったら出ていると見なしている（アドレスバーの伸び縮みでは反応しない大きさ）。外付けキーボードをつないだ端末など、文字盤が出ないのに高さが変わる状況では判定が外れることがある。外れても「固定をやめる」側に倒れるだけで、打てなくなることはない | 実機（iPhone・Android）で確かめる機会があれば、しきい値（0.75）を見直す |

---

## 本人向けの説明文が共通文に化けていた件（2026-08-12）

**L1 は解消済み。** 判定側が作った本人向けの説明が、本人に基準値を出さないための検査に弾かれ、
共通の言い換え文に差し替わっていた。同じ理由で化けていた文は昇格の理由だけでなく**4種類**あった
（判定の基準と直し方は `docs/product/spec.md` §19）。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| Q1 | **2026-08-12 より前に確定した評価は、いまも共通の言い換え文が出る** | 保存済みの文には古い言い回し（「昇格に必要な水準」）が入っており、検査はそれを弾く。**確定済みの評価の中身は書き換えない**という決まりを優先した。差し替え文のほうを「次に何をすればよいか」が伝わる書き方に直してある | このままでよい。過去の評価をどうしても直したいという声が出たら、そのときに「表示だけ作り直す」やり方を検討する（保存済みの文は触らない） |
| Q2 | **昇格要件の文言そのものは、いまも本人にそのまま見えている** | 昇格要件（「スキルアップ研修（チーフ以上）」など）は**本人が満たすべき自分の要件**なので、評価詳細の「昇格要件」一覧に出す設計になっている（今回変えていない）。一方、昇格できない理由の文では、要件名に「80点以上」のような基準値が書かれている場合だけ要件名を伏せ、「内容は上長にご確認ください」に置き換えた。**理由の文と一覧で見え方が違う**という不揃いが残る | 昇格要件の書き方の決まり（要件名に点数を書かない）を管理画面の注記として足すか、一覧側にも同じ伏せ方を当てるかを、制度設定の画面を触るときに決める |
| Q3 | **検査は「言葉」で見ており、意味では見ていない** | 危ない語（配点・満点・必要点数・昇格に必要 など）と数字の形を正規表現で見張っている。言い換えれば、**新しい言い回しで基準値を書けばすり抜ける**。ただし本人向けの文を作るのは3か所だけで、すべて試験で押さえてある | 本人向けの文を新しく増やすときは、必ず `src/lib/domain/employee-text-guard.test.ts` に足すこと（この試験が「作る側」と「ふるいにかける側」の食い違いを見張っている） |
| Q4 | **「差し替えが起きた」ことを記録に残していない** | 差し替えが起きても画面は成立するため、気づく手立ては試験しかない。記録を残すには保存先を用意する必要があり、今回の範囲を超える | 同じ化け方が別の形で疑われたら、差し替え回数を数える仕組みを検討する |

---

## 設問づくりとランク基準の「下限・上限」にも桁の上限を当てた件（2026-08-12）

**P1・P2 はこれで解消**（詳細は `docs/product/spec.md` §20）。

これまで 1兆の上限が当たっていたのは「回答の提出」と「取り込み」だけで、
設定を作る側（設問の下限・上限／ランク基準の下限・上限／選択肢の点数）は無制限だった。
実害が出ていなかったのは**別の仕組みがたまたま防いでいた**からで、
その仕組みを1つ動かせば穴が開く状態だった。受け口そのものに決まりを置き直した。

**新しい基準は作っていない。**既存の `MAX_ABS_NUMBER`（1兆）と既存の断り文を、
共通の入口 `checkNumberMagnitude` から使い回している。

既存データは本番で確かめた（ランク基準825行・165項目、設問3113問、選択肢の点数50個、
保存済みの回答すべて）。**弾かれるものは1件も無かった。**
確かめ方は `src/lib/domain/production-bounds.check.test.ts` に残してある。

- テスト件数: 1138件 → **1197件**。網羅率は文・分岐・関数・行すべて100%を維持。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| R1 | **下書きの自動保存だけは、いまも桁を見ていない** | 打っている途中の値に「保存できません」と言うと、何が起きたのか分からないまま入力が止まる。自動保存は本人が押していないので、断られたことにも気づけない。提出のときには必ず 1兆で止まるため、極端な値が確定した評価に入る道はふさがっている | 下書きの保存が断られたことを画面の隅に出せるようになったら、そのとき桁だけを見るようにする |
| R2 | **上限は「1兆」の1本きりで、欄ごとの実務的な上限は無い** | たとえば設問の上限に 5000億と書けても、いまは通る。桁の打ち間違い（10の20乗のような値）を止めるのが目的で、実務的にありえる大きさかどうかまでは見ていない。欄ごとに違う上限を作ると、経路ごとに通る値が変わって説明できなくなる | 「この項目は◯◯までのはず」という声が具体的に出てきたら、その欄にだけ `NumberFieldPolicy` の `max` を足す（上限そのものは1兆のまま） |
| R3 | **設問づくりの画面では、下限・上限の欄に上限を渡していない** | 画面の `NumberField` は `parseNumberInput` を通るので、1兆超えはその場で止まる（受け口と同じ決まり）。欄ごとに `max` を渡すと、その値が「入力の目安」として画面に出てしまい、1兆という桁の話が実務の目安のように読めてしまう | 欄ごとの実務的な上限（R2）を入れるときに、あわせて画面の案内文も見直す |
| R4 | **選択肢の点数は、桁の上限しか見ていない** | 点数の実務的な範囲（0〜3で使われている）を決め打ちにすると、行動指針以外の使い方が出てきたときに塞がる。いまは1兆を超える打ち間違いだけを止めている | 選択肢の点数の使われ方が固まったら、範囲を決めて `NumberFieldPolicy` に乗せる |

---

## ご本人にも「まだ確定していない」という事実を出した件（2026-08-12）

**N3 は解消済み。** 「評価の結果を見る」に、まだ確定していない期があることを出すようにした。
判定は §17 のものをそのまま使い（`buildStalledRows`）、本人向けには**削るだけ**の関数を足した。
考え方と言い方の決まりは `docs/product/spec.md` §21。

**出さなかったものと、その理由**（詳細は spec §21 の表）:

- **経過日数は出さない。** 本人は確定を自分で進められない。日数は焦りだけを増やし、できることを何も増やさない。
- **確定待ちと集計待ちは区別しない。** 本人から見ればどちらも「出したのに返ってこない」で同じ。期ごとに1件へ畳んだ。
- **催促の導線は付けない。** 事実だけを置く。
- **ログイン直後のホームには出さない。** ホームは「今やること」の場所で、本人がこの件でできることは無い。

- 本番の実データ: いま確定待ちは**0件**なので、本番の画面には何も出ない（0件のときは知らせ自体を描かない）。
  手元で「締め切ったのに未確定」を1期ぶん・2期ぶん作って、1期／複数期／該当なしの3通りを確かめた。
- 本人向けの2文は、Q3 で入った `src/lib/domain/employee-text-guard.test.ts` にも通してある
  （点数・配点・基準値をひとつも含まないため、検査は1文字も緩めずに通る）。
- テスト件数: 1208件 → **1221件**（新規13件。判定の削り方10件＋本人向け文のふるい3件）。
  計算まわり15ファイルの網羅率100%を維持。
- 触った**本人の画面のファイル**: `src/app/me/results/page.tsx` のみ
  （`src/app/me/page.tsx` と `MyDashboard.tsx` は触っていない。#39「昇格できない理由の文言」とは重ならない）。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| S1 | **本人は「いつごろ確定するか」を知る手立てがない** | 「◯月ごろ確定します」を出すには、期ごとに確定予定日を持たせる必要がある。いまの評価期間には終了日しかなく、確定予定日を新しく入力してもらう話になる。まず「まだ途中である」という事実だけを出して、予定日まで要るかを見たい | 「いつ出るのか分からない」という声が出たら、評価期間に確定予定日を足して、この知らせに添える |
| S2 | **知らせは、本人が「評価の結果を見る」を開いたときにしか出ない** | ホームに出すと「今やること」が押し下げられる（本人にできることは無いため）。メールで知らせるのは、本人にできることが無い以上、通知が増えるだけになりかねない | 本人から問い合わせが続くようなら、ホームの「自分の結果」の枠の中に小さく1行添えるかを検討する（枠の外・最上部には置かない） |
| S3 | **本人の画面には、まだ確定していない期の「回答した内容」への行き先が無い** | 回答そのものは `/me/responses/[id]` から見られるが、この知らせは期の単位でまとめており、回答は版ごとに複数あることがある。どれへ飛ばすかを決めきれなかった | 「自分が何を出したか見返したい」という声が出たら、期ごとに最後に提出した回答へ飛ばす行き先を足す |
| S4 | **「本人にできることは無い」という前提を、実際の運用で確かめていない** | 本番はいま確定待ち0件で、本人が待たされている実例がまだ無い。催促の導線を付けない判断も、日数を出さない判断も、いまは前任者の整理と今回の判断に基づくもの | 半期を1回まわして、待たされた本人が実際にどう動いたか（黙って待つ／上長に聞く／問い合わせる）を見てから、出す情報を足すか決める |


## 説明文の量と出し方をそろえた回（2026-08-12）

**発注者の指摘**「説明文・注意事項が多くて煩わしい」「1文が長い」「全部を1行で書かないでほしい」
「既定で出す必要があるか見直してほしい」「これは全ての画面で共通」。
さらに方向として「**情報を一旦引き算した上で、表示方法をどうプラスするか**。
ただし今使っている情報は全て反映させておくこと。削除・非表示にする方向はやめること」。

**やったこと**（考え方と決まりは `docs/product/spec.md` §22）:

- 1文40文字を `src/components/ui-rules.test.ts` で機械的に強制した。**除外ファイルは1つも作っていない。**
  差し込み（`${...}` / `{...}`）の中身は数えない（文の骨格だけを測る）。検査の網の広さ自体も検査した。
- ソースにある40文字超 **68文** を書き直し、**0件**にした。
- 畳む仕組みを3つ（その場で開く／まとめて1か所／窓）に限定し、
  **`<details>` を書いてよいのは `src/components/ui.tsx` だけ**にした。素の `<details>` 14か所を移した。
- 「畳んだものへ必ず手が届く」ことを検査で固めた（押す場所の必須化・`display:none` 禁止・
  押せる見た目・窓の「閉じる」必須）。**畳む＝隠す にしない。**
- 日本語の折り返しを `globals.css` の1か所に置き、`word-break: break-all` をどこにも書かせない検査を入れた。
- 項目名の列挙を文から出し、`<ul>` の並びで出すようにした（昇給・昇格の理由、使っている場所）。
- **のべ287画面**（4役割 × 全経路）を実際に描画して機械で走査した（`scripts/scan-rendered-text.mjs`）。
- テスト件数: 1221件 → **1246件**。計算まわり15ファイルの網羅率100%を維持。

**畳まなかったもの（既定で出し続けるもの）とその理由**:

- 取り返しのつかない操作の警告（「完全に消します。元に戻せません。」「いまのパスワードは使えなくなります。」）
  … 見落とすと損害が出る。畳んではいけない。
- 締切・公開済みかどうか・残り件数 … 知らずに操作すると事故る。
- 「できない」という事実そのものと、次の一歩1つ … 無言の読み取り専用を作らない。

**畳んだもの**: 制度の考え方・計算式の解説・手順や書式の説明・名前の列挙・全行で同じになる説明。

| # | 残っていること | なぜ今やらなかったか | 次にやること |
|---|---|---|---|
| T1 | **会社が登録した文章そのものは長いまま**（KPIの設問文・計算式・備考。のべ170か所） | 利用者自身が書いた内容で、こちらで書き換えるわけにいかない。折り返しで読めるようにするところまでが今回の範囲 | 設問や備考を入力する画面で「1文が40文字を超えています」と**やわらかく知らせる**（止めない）。実際に長い文が読みにくいという声が出てから入れる |
| T2 | **すでに確定した評価の理由文は、古い1行の形のまま**（のべ81か所） | 確定済みの評価は変えないという決まりがある。新しい形は次に確定する評価から出る | 何もしない。半期を1回まわして、新旧が混ざって見えることが問題になるかを見る |
| T3 | **描画して走査する道具は、手で走らせる前提**（preview 起動＋見本データが要る） | 自動で走らせるには、テストの中で preview を立ち上げて見本データを入れる仕組みが要る。今回の範囲では手で回して足りた | 画面を増やす回が続くようなら、CI で走らせる（先に `scripts/scan-rendered-text.mjs` の経路一覧を自動生成にする） |
| T4 | **畳んだものを「開いた状態」で見た目を確かめる検査が無い** | 開く仕組み（押す場所がある・消されていない・押せる見た目）は機械で固めたが、開いた中身の見た目までは見ていない | 「開いたときだけ崩れる」不具合が出たら、開いた状態を描画して確かめる検査を足す |
| T5 | **「引き算の線引き」は文章の決まりで、機械では見ていない** | 何を既定で出すかは画面ごとの判断で、機械に測らせると必ず例外だらけになる。spec §22-2 の表で言葉にするところまでにした | 同じ判断が3回以上繰り返されたら（例: 全行で同じ文は1か所へ）、その形だけを検査に足す |
